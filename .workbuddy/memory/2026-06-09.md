# 2026-06-09 工作记录

## WXHB 项目修复

### 问题1：run.bat / 安装依赖.bat 找不到 Python
- 项目目录无捆绑 Python，系统 PATH 也无 python
- 修复：`run.bat` 和 `安装依赖.bat` 增加检测 `C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe`
- `安装依赖.bat` 加了 `setlocal enabledelayedexpansion` 修复变量作用域问题

### 问题2：index.html 侧边栏 ComfyUI 设置和项目主页按钮
- 用户要求删除，已移除

### 问题3：api-settings.html ModelScope 图片损坏
- `modelscope.gif` / `modelscope-1.gif` 文件不存在，图片显示为损坏图标
- 修复：`renderProviderList()` 去掉特殊的 GIF 图片分支，统一使用文字卡片样式

### 问题4：视频生成返回非 JSON（云雾平台）
- 根因：`protocol: "openai"` 的平台被硬编码使用 `/v2/videos/generations`，云雾的正确路径是 `/v1/videos/generations`
- 修复 `main.py`：
  - `submit_url` 拼接：OpenAI 兼容 → `/v1/videos/generations`
  - `wait_for_video_task` 轮询 URL：OpenAI 兼容 → `/v1/videos/tasks/{task_id}`
  - 非 JSON 响应错误提示优化：提取 HTML title 给出友好提示
